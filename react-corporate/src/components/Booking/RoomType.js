import React, { Component } from 'react';

class RoomType extends Component {
  render() {
    return (
      <select defaultValue="3312">
        <option value="3312">Junior Suite</option>
      </select>
    );
  }
}

export default RoomType;
